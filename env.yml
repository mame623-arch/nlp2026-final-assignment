name: nlp_final
channels:
  - defaults
dependencies:
  - python=3.8
  - pip
  - pip:
      - torch
      - torchvision
      - torchaudio
      - tqdm==4.58.0
      - requests==2.25.1
      - importlib-metadata==3.7.0
      - filelock==3.0.12
      - sklearn==0.0
      - tokenizers==0.20
      - explainaboard_client==0.0.7
      - einops==0.8.0
      - transformers==4.46.3
      - sacrebleu==2.5.1
      - scikit-learn          # metrics, 스태커(LogisticRegression)
      - pandas                # data/get_ood_data.py, data/augment.py
      - datasets              # PAWS·MRPC 다운로드
      - matplotlib            # analysis/visualize_attention.py
      - openai                # analysis/judge.py (LLM judge; ~/.openai_key 필요)
      - sacremoses            # data/augment.py back-translation (MarianMT)